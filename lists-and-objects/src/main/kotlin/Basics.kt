fun main() {
    // Immutable list - you cannot add items after initialization
//    var shoppingList = listOf("Processor", "RAM", "Graphics Card", "SSD")

    // Mutable list - you can add items and modify
    var shoppingList = mutableListOf("Processor", "RAM", "Graphics Card", "SSD")
}
